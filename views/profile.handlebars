<h1>Welcome</h1>
{{#each user as |user| }}
<div class="row mb-4 Home">
  <div class="col-md-5">
    <h2>User Info
    </h2>
    <p>{{first_name}}</p>
    <p>{{last_name}}</p>
    <p>{{email}}</p>
    <p>{{primaryPhone}}</p>
    <p>{{cellPhone}}</p>
    <p>{{workPhone}}</p>
  </div>
</div>
{{/each}}
<script src="/js/profile.js"></script>